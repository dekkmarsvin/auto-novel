# 2026-05-26 Upstream Sync Through 19f2ae18

This record documents a selective sync of approved upstream commits after
`b279082ddc5a86af46dbf1abd027e8ac4fe20821` through
`19f2ae180108541136be70c886f75bb03a2a45cb`.

## Sync Baseline

- Fork head before sync branch work: `4c98fc22f3b9898755648bd6fb6aa7d164222f33`
- Upstream evaluated through: `19f2ae180108541136be70c886f75bb03a2a45cb`
- Previous upstream sync point: `b279082ddc5a86af46dbf1abd027e8ac4fe20821`
- Candidate upstream range evaluated: `b279082ddc5a86af46dbf1abd027e8ac4fe20821..19f2ae180108541136be70c886f75bb03a2a45cb`

For the next upstream sync, start by reviewing new upstream commits after:
`19f2ae180108541136be70c886f75bb03a2a45cb`.

## Manual Decisions

- No new manual product or fork-policy decision was required. Both candidate
  commits were accepted as direct upstream cherry-picks because they only touch
  GPT Pipeline workspace and translator queue execution behavior.

## Accepted Upstream Commits

- `069ed1c5d7100a77e415657f7471506050ac5bd4` fix(web): 支持高水位限制，修复竞态问题 (#384)
- `19f2ae180108541136be70c886f75bb03a2a45cb` feat(web): 为pipeline工作区添加“重试失败任务”按钮

## Rejected Or Deferred Changes

- No candidate commit was rejected or deferred in this sync.
- Direct upstream removal of fork docs, ThemeGlossary, guardrails, and
  `docker-compose.dev.yml` remains rejected by policy.

## Fork Adaptations

- No fork-adapted code changes were needed. Both accepted commits were applied
  with `git cherry-pick -x`.
- ThemeGlossary backend and frontend files, DTO fields, repository injection,
  and glossary merge order were not touched by this candidate range.
- Fork production domain overrides remain `books.kotoban.top`; upstream
  `n.novelia.cc` was not introduced by this candidate range.
- `docker-compose.dev.yml`, `AGENTS.md`, `CONTEXT.md`, sync docs, and guardrail
  scripts were preserved.

## Patch-Equivalence Notes

- Both accepted upstream commits are direct cherry-picks with upstream source
  commit references in the commit messages.

## Validation

- `.\scripts\check-fork-invariants.ps1` passed with existing local warnings for
  ignored generated frontend declarations.
- `.\scripts\check-selective-feature-sync.ps1 -BaseRef origin/main -HeadRef HEAD`
  passed with the same existing generated declaration warnings.
- `git diff --check origin/main..HEAD` passed.
- `pnpm build` initially failed in the sandbox with `spawn EPERM`, then the
  external run exposed a missing local `daemon/node_modules` install. `pnpm
install` completed with the lockfile already up to date, and the rerun of
  `pnpm build` passed. The web build reported the existing large chunk warning.
- `docker compose -f docker-compose.dev.yml build web api` passed. The web image
  build used `pnpm v10.33.4`; both `auto-novel-web:latest` and
  `auto-novel-api:latest` were built.

## Post-Push GitHub Actions

- Pull request #19 checks passed:
  - `selective-feature-sync`
  - `workspace-build`
  - `build`

## Next Sync Checklist Addendum

1. Fetch upstream.
2. Compare new candidates from the upstream SHA recorded above.
3. Read the latest `docs/sync/` record before interpreting `git cherry`.
4. Preserve every Fork Capability listed in `AGENTS.md`.
5. Run the normal Upstream Merge Checklist, including Docker build for container
   changes and GitHub Actions checks after push.
6. For fork-adapted upstream sync commits that are not direct `git cherry-pick -x`
   commits, include a `Sync-Manifest: docs/sync/YYYY-MM-DD*.md` trailer.
7. Merge sync PRs with a normal fork PR merge commit or a commit that preserves
   Sync Manifest evidence; do not merge `upstream/main` directly.
