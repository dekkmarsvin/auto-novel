# 2026-05-29 Upstream Sync Through 3e5b7a5d

This record documents a selective sync of approved upstream commits after
`19f2ae180108541136be70c886f75bb03a2a45cb` through
`3e5b7a5daca93da0bf195275cb6c0cf2a3e667ad`.

## Sync Baseline

- Fork head before sync branch work: `ac5e12f177264ce0678b43261f5cef965598aacb`
- Upstream evaluated through: `3e5b7a5daca93da0bf195275cb6c0cf2a3e667ad`
- Previous upstream sync point: `19f2ae180108541136be70c886f75bb03a2a45cb`
- Candidate upstream range evaluated: `19f2ae180108541136be70c886f75bb03a2a45cb..3e5b7a5daca93da0bf195275cb6c0cf2a3e667ad`

For the next upstream sync, start by reviewing new upstream commits after:
`3e5b7a5daca93da0bf195275cb6c0cf2a3e667ad`.

## Manual Decisions

- Accepted `1088e9d3fe39162f35df83a26b7d7cebc9090af6` remote login proxy
  behavior.
- Allowed a fork-adapted implementation for `1088e9d3` because the upstream
  patch uses `auth.novelia.cc` and conflicts with the fork's
  `books.kotoban.top` / `auth.kotoban.top` domain policy.
- Domain substitutions did not require separate product approval; upstream
  `novelia.cc` references were replaced with the fork's kotoban domains.

## Accepted Upstream Commits

- `3e5b7a5daca93da0bf195275cb6c0cf2a3e667ad` fix(web): 修复 GPT 工作区 BETA 重新排队后不翻译新增章节的问题 (#387)
- `1088e9d3fe39162f35df83a26b7d7cebc9090af6` feat(web): 添加远程登录代理配置 (#388), accepted as a fork-adapted change rather than a direct cherry-pick.

## Rejected Or Deferred Changes

- Direct upstream merge remains rejected. `main..upstream/main` still deletes
  ThemeGlossary files, fork docs, sync guardrails, and `docker-compose.dev.yml`.
- Upstream `n.novelia.cc` / `auth.novelia.cc` domain targets remain rejected and
  were replaced with `books.kotoban.top` / `auth.kotoban.top` where applicable.
- No candidate commit in `19f2ae18..3e5b7a5d` was deferred.

## Fork Adaptations

- `3e5b7a5d` was applied with `git cherry-pick -x`.
- `1088e9d3` was applied manually:
  - `web/src/pages/Auth.vue` now compares message origin with
    `new URL(AuthUrl).origin` and builds iframe query parameters through
    `URLSearchParams`.
  - `web/src/util/useUserData/api.ts` uses `/auth-proxy` when
    `VITE_API_MODE=remote`, otherwise it targets `https://auth.kotoban.top`.
  - `web/vite.config.ts` adds the remote auth proxy using
    `https://auth.kotoban.top`.
- ThemeGlossary backend and frontend files, DTO fields, repository injection,
  and glossary merge order were not touched by this candidate range.
- `docker-compose.dev.yml`, `AGENTS.md`, `CONTEXT.md`, sync docs, and guardrail
  scripts were preserved.

## Patch-Equivalence Notes

- `3e5b7a5d` is patch-equivalent through direct cherry-pick provenance.
- `1088e9d3` is intentionally not patch-equivalent because the fork preserves
  kotoban production domains instead of upstream novelia domains.

## Validation

- `.\scripts\check-fork-invariants.ps1` passed with existing local warnings for
  ignored generated frontend declarations.
- `pnpm --filter @auto-novel/web test -- workspaceJob.test.ts` passed:
  `web/tests/workspaceJob.test.ts` reported 3 passing tests.
- `pnpm --filter @auto-novel/web build` passed. The web build reported the
  existing large chunk warning.

## Next Sync Checklist Addendum

1. Fetch upstream.
2. Compare new candidates from the upstream SHA recorded above.
3. Read the latest `docs/sync/` record before interpreting `git cherry`.
4. Preserve every Fork Capability listed in `AGENTS.md`.
5. Run the normal Upstream Merge Checklist, including Docker build for container
   changes and GitHub Actions checks after push.
6. For fork-adapted upstream sync commits that are not direct `git cherry-pick -x`
   commits, include a `Sync-Manifest: docs/sync/YYYY-MM-DD*.md` trailer.
7. Merge sync PRs with a normal fork PR merge commit or a commit that preserves
   Sync Manifest evidence; do not merge `upstream/main` directly.
