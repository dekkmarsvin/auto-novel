# 2026-06-08 Upstream Sync Through 54f382b2

This record documents a selective sync evaluation of approved upstream commits
after `53ffcac5283992361facdfaef0a82a593c2e92fb` through
`54f382b256a3ed5969dfd3cfa8b159bdf4683d41`.

## Sync Baseline

- Fork head before sync branch work: `8f666546dd6b970e9dc2dc1931e213d82330151a`
- Upstream evaluated through: `54f382b256a3ed5969dfd3cfa8b159bdf4683d41`
- Previous upstream sync point: `53ffcac5283992361facdfaef0a82a593c2e92fb`
- Merge base with upstream: `ef6731688b7bfa669b6ae4dfe98007eaaf01e586`
- Candidate upstream range evaluated: `53ffcac5283992361facdfaef0a82a593c2e92fb..54f382b256a3ed5969dfd3cfa8b159bdf4683d41`

For the next upstream sync, start by reviewing new upstream commits after:
`54f382b256a3ed5969dfd3cfa8b159bdf4683d41`.

## Manual Decisions

- No new product or fork-policy decision was required.
- Both candidate commits are small frontend bug fixes and do not touch
  ThemeGlossary, domain overrides, Docker image ownership, dev compose files,
  generated declaration handling, or selective-sync guardrails.

## Accepted Upstream Commits

- `41850e0dd39c083c0cb456b414d6e8328209dbec` fix(web): 修复阅读器中切换颜色预设时，颜色选择器不更新的问题 (#397)
- `54f382b256a3ed5969dfd3cfa8b159bdf4683d41` fix(web): 修复工作区添加翻译器的modal不自动关闭的问题 (#398)

## Rejected Or Deferred Changes

- No candidate commits in `53ffcac5..54f382b2` were rejected or deferred.
- Direct upstream merge remains rejected. The fork continues to preserve
  ThemeGlossary, fork domain overrides, `docker-compose.dev.yml`, fork docs,
  and selective-sync guardrails instead of merging upstream wholesale.

## Fork Adaptations

- `41850e0d` was applied with `git cherry-pick -x`.
- `54f382b2` was applied with `git cherry-pick -x`.
- ThemeGlossary backend and frontend files, DTO fields, repository injection,
  and glossary merge order were not touched by this candidate range.
- `books.kotoban.top` domain overrides, `docker-compose.dev.yml`, generated
  frontend declaration ignore policy, and fork-owned Docker image behavior were
  not touched by this candidate range.

## Patch-Equivalence Notes

- `41850e0d` and `54f382b2` are patch-equivalent through direct cherry-pick
  provenance.

## Validation

- `.\scripts\check-fork-invariants.ps1` passed.
- `.\scripts\check-selective-feature-sync.ps1 -BaseRef origin/main -HeadRef HEAD` passed.
- `corepack pnpm install --frozen-lockfile --ignore-scripts` completed for the
  isolated worktree because `pnpm` was not directly available on PATH and the
  worktree did not yet have `node_modules`.
- `corepack pnpm --stream -r run build` passed. The web build reported the
  existing large chunk and plugin timing warnings.

## Post-Push GitHub Actions

- Not run. This evaluation branch has not been pushed.

## Next Sync Checklist Addendum

1. Fetch upstream.
2. Compare new candidates from the upstream SHA recorded above.
3. Read the latest `docs/sync/` record before interpreting `git cherry`.
4. Preserve every Fork Capability listed in `AGENTS.md`.
5. Run the normal Upstream Merge Checklist, including Docker build for container
   changes and GitHub Actions checks after push.
6. For fork-adapted upstream sync commits that are not direct `git cherry-pick -x`
   commits, include a `Sync-Manifest: docs/sync/YYYY-MM-DD*.md` trailer.
7. Merge sync PRs with a normal fork PR merge commit or a commit that preserves
   Sync Manifest evidence; do not merge `upstream/main` directly.
