# 2026-05-22 Upstream Sync Through 800dc4f5

This record documents a fork-adapted selective sync of upstream commits after
`e769d6c9c088bbae743ca71cef8d19245b95dab0` through
`800dc4f5a1391743e98135b4ca228ade391c2c64`.

## Sync Baseline

- Fork head before sync branch work: `a99f5388e8f3c17609670400695b84e7c5b17cab`
- Origin head before sync: `a122378b4b4d70861d806086f0c84e9ec0017116`
- Upstream evaluated through: `800dc4f5a1391743e98135b4ca228ade391c2c64`
- Merge base with upstream: `ef6731688b7bfa669b6ae4dfe98007eaaf01e586`
- Candidate upstream range evaluated: `e769d6c9c088bbae743ca71cef8d19245b95dab0..800dc4f5a1391743e98135b4ca228ade391c2c64`

For the next upstream sync, start by reviewing new upstream commits after:
`800dc4f5a1391743e98135b4ca228ade391c2c64`.

## Accepted Upstream Commits

- `407b265c24740cad4c322ecd0f79c70bb6a4fe1f` fix(web): 修复openai-translator的正则 (#370)
- `fb98eccc3d072ac0fafe1dd9e07513bb97b34548` fix(web): 为 Amazon 导入添加伪装 (#371)
- `a5e0a813c3f48b38e0374cdd4fab64b9f549b1f2` fix(web): 修复 Headers 类型编译错误
- `61354c9a1092afc7f59a509395df86a35952f166` feat(web): 补齐章节原文更新接口
- `800dc4f5a1391743e98135b4ca228ade391c2c64` refactor(crawler): 补充 Amazon 返回类型定义

## Fork Adaptations

- `407b265c` was applied with the fork's parser fix intact: the separator group is
  now non-capturing and translated content is read from `match[2]`.
- `fb98eccc` did not replace root `package.json` because the fork already had the
  verified `dev:web` command and must preserve `check:fork` and `check:sync`.
- `fb98eccc` and `a5e0a813` were combined into the final `normalizeHeaders`
  helper shape before commit, avoiding the intermediate `toHeaderDict` helper.
- `61354c9a` was cherry-picked with `-x`; the existing `themeGlossaryId`
  handling in `WebNovelApi.ts` was preserved.
- `800dc4f5` was cherry-picked with `-x` and adds typed Amazon crawler return
  models without touching fork-only ThemeGlossary code.

## Rejected Changes

- No candidate commit was rejected.
- No upstream production-domain change was present in this candidate range.
- No candidate commit deleted `docker-compose.dev.yml` or generated frontend
  declaration ignore policy.

## Validation

- `.\scripts\check-fork-invariants.ps1` passed.
- `.\scripts\check-selective-feature-sync.ps1 -BaseRef origin/main -HeadRef HEAD`
  passed.
- `git diff --check origin/main..HEAD` passed.
- `pnpm --filter '@auto-novel/translator' test -- openai_prompt.test.ts`
  passed.
- `pnpm --stream --filter '@auto-novel/web^...' run build` passed.
- `pnpm --filter '@auto-novel/web' run build` passed with the existing large
  chunk and plugin timing warnings.
- `pnpm --stream -r run build` initially failed with an Rspack panic:
  `Dependency with ID DependencyId(1) not found`. Re-running package builds after
  deleting ignored `node_modules/.cache/rspack` directories inside the isolated
  worktree passed, and the subsequent full workspace build passed.

## Post-Push GitHub Actions

- Not pushed yet.
