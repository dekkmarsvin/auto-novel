# 2026-06-26 Upstream Sync Through b925472e

This record documents a selective sync evaluation of approved upstream commits
after `236af5b7ba6e0b47bf7de082f344f489d7867494` through
`b925472e789820e6ce55d8db5bd4680609ae9d78`.

## Sync Baseline

- Fork head before sync branch work: `ae8a6d6389b05b26ce9fd66db0d623ed9f676f58`
- Origin head before sync branch work: `ae8a6d6389b05b26ce9fd66db0d623ed9f676f58`
- Upstream evaluated through: `b925472e789820e6ce55d8db5bd4680609ae9d78`
- Previous upstream sync point: `236af5b7ba6e0b47bf7de082f344f489d7867494`
- Merge base with upstream: `ef6731688b7bfa669b6ae4dfe98007eaaf01e586`
- Candidate upstream range evaluated: `236af5b7ba6e0b47bf7de082f344f489d7867494..b925472e789820e6ce55d8db5bd4680609ae9d78`

For the next upstream sync, start by reviewing new upstream commits after:
`b925472e789820e6ce55d8db5bd4680609ae9d78`.

## Manual Decisions

- No new product or fork-policy decision was required.
- The only candidate commit removes an unused proxy-domain option from the
  package-level Hameln crawler and does not touch ThemeGlossary, fork domain
  overrides, Docker image ownership, dev compose files, generated declaration
  handling, readable Baidu translation compatibility, or selective-sync
  guardrails.

## Accepted Upstream Commits

- `b925472e789820e6ce55d8db5bd4680609ae9d78` refactor(crawler): 不再使用hameln代理域名

## Rejected Or Deferred Changes

- No candidate commits in `236af5b7..b925472e` were rejected or deferred.
- Direct upstream merge remains rejected. `git merge-tree` still shows wide
  fork divergence across ThemeGlossary, fork domain, Baidu readable translation
  source compatibility, CI/runtime choices, and other previously curated
  fork-owned areas.

## Fork Adaptations

- `b925472e` was applied with `git cherry-pick -x`.
- The patch removes the package crawler's `URL_PROXY`, `useProxy` option,
  `baseUrl` getter, and `setOptions` method from `packages/crawler/src/web/hameln.ts`.
- The package crawler now fetches Hameln metadata and chapters directly from
  `https://syosetu.org`.
- The server-side Kotlin Hameln provider and `HAMELN_TOKEN` wiring were not
  touched by this upstream candidate range.
- ThemeGlossary backend and frontend files, DTO fields, repository injection,
  and glossary merge order were not touched by this candidate range.
- `books.kotoban.top` domain overrides, `docker-compose.dev.yml`, generated
  frontend declaration ignore policy, and fork-owned Docker image behavior were
  not touched by this candidate range.

## Patch-Equivalence Notes

- `b925472e` is patch-equivalent through direct cherry-pick provenance.

## Validation

- `.\scripts\check-fork-invariants.ps1` passed with warnings that generated
  frontend declarations exist locally.
- `.\scripts\check-selective-feature-sync.ps1 -BaseRef origin/main -HeadRef HEAD`
  passed with the same generated declaration warnings.
- `git diff --check origin/main..HEAD` passed.
- `corepack pnpm --stream -r run build` passed. The web build reported the
  existing large chunk warning.
- `docker compose -f docker-compose.dev.yml build web api` did not run because
  Docker Desktop's Linux engine was unavailable:
  `open //./pipe/dockerDesktopLinuxEngine: The system cannot find the file specified`.

## Post-Push GitHub Actions

- Not run. This local sync branch has not been pushed.

## Next Sync Checklist Addendum

1. Fetch upstream.
2. Compare new candidates from the upstream SHA recorded above.
3. Read the latest `docs/sync/` record before interpreting `git cherry`.
4. Preserve every Fork Capability listed in `AGENTS.md`.
5. Run the normal Upstream Merge Checklist, including Docker build for container
   changes and GitHub Actions checks after push.
6. For fork-adapted upstream sync commits that are not direct `git cherry-pick -x`
   commits, include a `Sync-Manifest: docs/sync/YYYY-MM-DD*.md` trailer.
7. Merge sync PRs with a normal fork PR merge commit or a commit that preserves
   Sync Manifest evidence; do not merge `upstream/main` directly.
